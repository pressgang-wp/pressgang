<?php

/**
 * Remove Nodes Configuration
 *
 * This configuration file defines the IDs of nodes (items) to be removed from the WordPress admin toolbar.
 * Each entry in the array is the ID of a toolbar node that will be removed.
 *
 * Example configuration array:
 * return [
 *     'wp-logo',          // Removes the WordPress logo
 *     'about',            // Removes the 'About WordPress' link
 *     'comments',         // Removes the 'Comments' link
 *     'new-content',      // Removes the 'New' menu
 *     'wporg',            // Removes the 'WordPress.org' link
 *     ... additional node IDs ...
 * ];
 *
 * @var array
 */

return [];
