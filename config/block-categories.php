<?php

/**
 * Blocks Categories Configuration
 *
 * This configuration file defines an array of custom categories available for each block.
 *
 * The 'category' array allows categorization of the block in the block editor and can be used
 * to group related blocks together. The array keys provide the category slug used when registering the block,
 * and the values provide the category title.
 *
 * Example:
 * [
 *     'slug' => 'Title',
 *     // ...
 * ]
 *
 * @see https://developer.wordpress.org/reference/hooks/block_categories_all/
 * @see https://developer.wordpress.org/reference/functions/register_block_type/
 *
 * @var array<string, string> An associative array of category slugs and their corresponding titles.
 */
return [];
